[
<% @articles.each do |article, index| %>
	{
		"id": <%= article.id %>,
		"name": <%= article.name.to_json.html_safe %>,
		"description": <%= article.description.to_json.html_safe %>,
		"price": "<%= article.price %>",
		"size": "TODO",
		"picture_url": <% if article.picture? %>"<%= article.picture.url %>"<% else %>null<% end %>
	}
	<% if not index == @category.article.count - 1 %>
	,
	<% end %>
<% end %>
]